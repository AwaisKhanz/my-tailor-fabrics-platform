// Legacy local DTO placeholder.
// Canonical request contract lives in shared package and is consumed directly by controller/service.
export type { CreateRateCardInput as CreateRateDto } from '@tbms/shared-types';
